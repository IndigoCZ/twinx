<h1>Listing categories</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Race</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.title %></td>
    <td><%= category.title %></td>
    <td><%= link_to 'Show', race_category_path(@current_race,category) %></td>
    <td><%= link_to 'Edit', edit_race_category_path(@current_race,category) %></td>
    <td><%= link_to 'Destroy', race_category_path(@current_race,category), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_race_category_path(@current_race) %>
