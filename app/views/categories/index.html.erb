<h1>Přehled Kategorií</h1>

<table>
  <tr>
    <th>Název</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.title %></td>
    <td><%= link_to 'Zobrazit', race_category_path(@current_race,category) %></td>
    <td><%= link_to 'Upravit', edit_race_category_path(@current_race,category) %></td>
    <td><%= link_to 'Smazat', race_category_path(@current_race,category), method: :delete, data: { confirm: 'Jste si jisti?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nová Kategorie', new_race_category_path(@current_race) %>
