<p id="notice"><%= notice %></p>

<h2><%= @category.title %></h2>
<%  @category.constraints.each do |c| %>
<p><%= c.description %></p>
<%  end %>

<div class="btn-group">
  <%= link_to 'Upravit', edit_race_category_path(@current_race,@category), class:"btn btn-inverse" %>
  <%= link_to 'Zpět', race_categories_path(@current_race), class:"btn" %>
</div>
